<article class="grid_12">
    <h1>Rozpis zapasu A</h1>
    <table>
        <tr>
            <td>Datum</td><td>Domaci</td><td>Hoste</td>
            <td>Hala</td><td>Vysledek</td>
            
            {if ($member)}
            <td>Detail</td>
            {/if}
        </tr>

        {foreach $match in $matchesA}
        <tr>
            <td>{echo date("d. m. Y", strtotime($match->date))}</td>
            <td>{echo $match->home}</td>
            <td>{echo $match->host}</td>
            <td>{echo $match->hall}</td>

            {if $match->scoreHome != -1}
            <td>{echo $match->scoreHome} - {echo $match->scoreHost}</td>
            {/if}
            {else}
            <td>neodehrano</td>
            {/else}
            {if ($member)}
            <td><a href="/zapasy/detail/{echo $match->id}">Detail</a></td>
            {/if}
        </tr>
        {/foreach}
    </table>
</article>

<article class="grid_12">
    <h1>Rozpis zapasu B</h1>
    <table>
        <tr>
            <td>Datum</td><td>Domaci</td><td>Hoste</td>
            <td>Hala</td><td>Vysledek</td>
            {if ($member)}
            <td>Detail</td>
            {/if}
        </tr>

        {foreach $match in $matchesB}
        <tr>
            <td>{echo date("d. m. Y", strtotime($match->date))}</td>
            <td>{echo $match->home}</td>
            <td>{echo $match->host}</td>
            <td>{echo $match->hall}</td>

            {if $match->scoreHome != -1}
            <td>{echo $match->scoreHome} - {echo $match->scoreHost}</td>
            {/if}
            {else}
            <td>neodehrano</td>
            {/else}
            {if ($member)}
            <td><a href="/zapasy/detail/{echo $match->id}">Detail</a></td>
            {/if}
        </tr>
        {/foreach}
    </table>
</article>